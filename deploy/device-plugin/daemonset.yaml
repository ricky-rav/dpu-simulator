apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: dpu-sim-device-plugin
  namespace: kube-system
  labels:
    app: dpu-sim-device-plugin
spec:
  selector:
    matchLabels:
      app: dpu-sim-device-plugin
  template:
    metadata:
      labels:
        app: dpu-sim-device-plugin
    spec:
      # Only run on nodes labeled as DPU-host workers.
      nodeSelector:
        k8s.ovn.org/dpu-host: ""
      hostNetwork: true
      tolerations:
        - operator: Exists
      priorityClassName: system-node-critical
      containers:
        - name: device-plugin
          image: DPU_SIM_DP_IMAGE
          imagePullPolicy: IfNotPresent
          env:
            - name: MGMT_PORT_VFS_COUNT
              value: "DPU_SIM_MGMT_PORT_VFS_COUNT"
            - name: UPLINK_VFS_COUNT
              value: "DPU_SIM_UPLINK_VFS_COUNT"
            - name: NUM_PAIRS
              value: "DPU_SIM_NUM_PAIRS"
          securityContext:
            privileged: true
          volumeMounts:
            - name: device-plugin-socket
              mountPath: /var/lib/kubelet/device-plugins
            - name: device-info
              mountPath: /var/run/k8s.cni.cncf.io/devinfo/dp
            # Kubelet's PodResources socket, used by the health checker to
            # tell in-use devices apart from destroyed ones.
            - name: pod-resources
              mountPath: /var/lib/kubelet/pod-resources
              readOnly: true
      volumes:
        - name: device-plugin-socket
          hostPath:
            path: /var/lib/kubelet/device-plugins
        - name: device-info
          hostPath:
            path: /var/run/k8s.cni.cncf.io/devinfo/dp
            type: DirectoryOrCreate
        - name: pod-resources
          hostPath:
            path: /var/lib/kubelet/pod-resources
            # Owned by kubelet: fail the mount if it is missing rather than
            # silently creating an empty directory with no socket in it.
            type: Directory
